# Websites Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rebuild the full website layer — consulting.b.studio newsletter-first redesign, capital.b.studio additions, build.b.studio (new repo), b.studio hub redesign, and bibelhausen.com (new repo).

**Architecture:** Five GitHub Pages sites. All five depend on `docs/pub-ids.md` from the newsletter infra plan — that file must exist before Task 3 (beehiiv embeds). Billboard design system throughout all Studio B properties (see design constants below). bibelhausen.com uses a distinct minimal identity.

**Tech Stack:** Static HTML/CSS/vanilla JS, GitHub Pages, Billboard design system (Fraunces 900, JetBrains Mono, Inter), Intersection Observer for scroll reveals, HubSpot forms/meeting links.

**Dependency:** Run the newsletter infrastructure plan first. The `docs/pub-ids.md` file it produces contains the beehiiv pub IDs needed for embed code in Tasks 3, 4, and 5.

---

## Design Constants (apply to all Billboard sites)

```
Colors:
  --yolk: #F7D344
  --ink: #0D0D0D
  --vermillion: #D94425
  --forest: #1A3328
  --paper: #F4F1EB

Fonts (Google Fonts):
  Fraunces: ital,opsz,wght@0,9..144,900;1,9..144,900
  Inter: wght@400;600;800
  JetBrains Mono: wght@500;700

Font URL:
  https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,900;1,9..144,900&family=Inter:wght@400;600;800&family=JetBrains+Mono:wght@500;700&display=swap

Diagonal stripe: repeating-linear-gradient(-45deg, var(--ink) 0, var(--ink) 8px, var(--yolk) 8px, var(--yolk) 16px)
Forest stripe: same but yolk/forest
```

**HubSpot portal ID:** `49070660`  
**HubSpot contact form ID:** `ea2c2f4a-...` (check b.studio contact section for full ID)  
**HubSpot meeting link (Consulting):** `https://meetings.hubspot.com/kbibelhausen`  
**HubSpot meeting link (Capital LP):** `https://meetings.hubspot.com/bibelhausen/studio-b-capital-lp-inquiry`

---

## File Structure

**Repos touched:**
- Modify: `studio-b-ai/consulting-b-studio` — `index.html` (redesign)
- Modify: `studio-b-ai/capital-b-studio` — `index.html` (add DLI embed + Dispatch cross-subscribe + remove meeting iframe)
- Create: `studio-b-ai/build-b-studio` — `index.html`, `CNAME` (new repo)
- Modify: `studio-b-ai/b-studio-website` — `index.html` (hub redesign: fix stats, fix Amplify tag, replace §03 with entity lanes)
- Create: `kbibelhausen/bibelhausen-com` — `index.html`, `CNAME` (new repo, personal account)

---

## Task 1: consulting.b.studio — newsletter-first redesign

**Files:**
- Modify: `studio-b-ai/consulting-b-studio/index.html`

The current architecture: caution stripe → hero ("Book a call" CTA) → 3 work cards → proof section → HubSpot meeting embed (full width iframe). The new architecture replaces the hero CTA, adds a Dispatch subscribe section after the hero, converts the meeting embed to a plain text link.

**NOTE:** The beehiiv embed for this task requires the Dispatch pub ID from `docs/pub-ids.md`. If that file doesn't exist yet, complete the newsletter infrastructure plan first, then return here.

- [ ] **Step 1: Write failing test (visual regression snapshot)**

Create `tests/consulting-homepage.test.js`:

```javascript
// Manual visual check — run: open index.html in browser
// Confirm:
// 1. Hero CTA says "Read Dispatch →" not "Book a call"
// 2. Dispatch subscribe section is present between hero and work cards
// 3. No iframe src containing "meetings.hubspot.com" in the DOM
// 4. Plain text meeting link is present: "meetings.hubspot.com/kbibelhausen"
// 5. Scroll reveal: sections start invisible, fade in on scroll
const assertions = [
  'hero CTA text = "Read Dispatch →"',
  'subscribe section exists before .work-section',
  'no .meetings-iframe-container with data-src',
  'text "meetings.hubspot.com/kbibelhausen" present',
];
console.log('Manual checklist:', assertions);
```

Run: `cat tests/consulting-homepage.test.js`  
Expected: File exists with checklist printed.

- [ ] **Step 2: Replace hero CTA — "Book a call" → "Read Dispatch →"**

In `index.html`, find the hero CTA link (currently pointing to `brief.benchmarks.b.studio/subscribe`). Replace the entire `<a>` tag:

Old:
```html
<a href="https://brief.benchmarks.b.studio/subscribe" class="cta">Get the brief →</a>
```

New:
```html
<a href="https://dispatch.b.studio/subscribe" class="cta">Read Dispatch →</a>
```

- [ ] **Step 3: Add Dispatch subscribe section after the hero**

After the closing `</section>` of the hero section (before the first `<div class="stripe">`), insert this full section. Replace `DISPATCH_PUB_ID` with the pub ID from `docs/pub-ids.md`:

```html
<!-- DISPATCH SUBSCRIBE -->
<section class="subscribe-section" id="subscribe">
  <div class="subscribe-inner">
    <div class="subscribe-kicker">DISPATCH, BY STUDIO B</div>
    <h2 class="subscribe-headline">Field notes from inside the operation.</h2>
    <p class="subscribe-body">War stories and operational patterns from active engagements — anonymized, direct, and written for operators who want to know what's actually working. Not what consultants are selling.</p>
    <div class="beehiiv-embed-container">
      <iframe src="https://embeds.beehiiv.com/DISPATCH_PUB_ID?slim=true" data-test-id="beehiiv-embed"
        height="52" frameborder="0" scrolling="no" style="margin: 0; border-radius: 0px !important;
        background-color: transparent; width: 100%;"></iframe>
    </div>
    <p class="subscribe-secondary">Kevin also publishes <a href="https://dli.b.studio/subscribe">DLI, by Studio B</a> — the open lower middle market lending index.</p>
  </div>
</section>
```

Add CSS for this section (add to `<style>` block):

```css
/* Subscribe section */
.subscribe-section { background: var(--ink); color: var(--yolk); padding: 4rem; border-bottom: 3px solid var(--ink); }
.subscribe-inner { max-width: 640px; }
.subscribe-kicker {
  font-family: 'JetBrains Mono', monospace; font-size: 0.5rem; font-weight: 700;
  letter-spacing: 0.2em; text-transform: uppercase; color: rgba(247,211,68,0.5); margin-bottom: 1.5rem;
}
.subscribe-headline {
  font-family: 'Fraunces', serif; font-weight: 900; font-style: italic;
  font-size: clamp(1.8rem, 3.5vw, 2.8rem); line-height: 1.05; letter-spacing: -0.02em;
  color: var(--yolk); margin-bottom: 1rem;
}
.subscribe-body { font-size: 0.92rem; line-height: 1.65; color: rgba(247,211,68,0.72); margin-bottom: 2rem; }
.beehiiv-embed-container { margin-bottom: 1.25rem; }
.subscribe-secondary { font-size: 0.78rem; color: rgba(247,211,68,0.45); }
.subscribe-secondary a { color: rgba(247,211,68,0.65); text-decoration: none; }
.subscribe-secondary a:hover { color: var(--yolk); }
```

- [ ] **Step 4: Replace HubSpot meeting iframe with plain text link**

Find and remove the meeting iframe block (current code):
```html
<div class="meetings-iframe-container" data-src="https://meetings.hubspot.com/kbibelhausen?embed=true&color=F7D344"></div>
```

Also find and remove any associated script tag that loads HubSpot meetings:
```html
<script type="text/javascript" src="https://static.hsappstatic.net/MeetingsEmbed/ex/MeetingsEmbedCode.js"></script>
```

Replace with this engagement section (before the footer):

```html
<!-- ENGAGEMENT -->
<section class="engagement-section" id="get-in-touch">
  <div class="engagement-inner">
    <div class="engagement-kicker">READY TO GO DEEPER?</div>
    <h2 class="engagement-headline">Most engagements start with a 30-minute call.</h2>
    <p class="engagement-body">We take a small number of engagements each year. If you're a PE sponsor, portco, or operator who wants to go further than the newsletter — here's where to start.</p>
    <a href="https://meetings.hubspot.com/kbibelhausen" class="engagement-link" target="_blank" rel="noopener">
      Book a 30-minute call → meetings.hubspot.com/kbibelhausen
    </a>
  </div>
</section>
```

Add CSS:

```css
/* Engagement section */
.engagement-section { background: var(--yolk); border-top: 3px solid var(--ink); padding: 4rem; }
.engagement-inner { max-width: 640px; }
.engagement-kicker {
  font-family: 'JetBrains Mono', monospace; font-size: 0.5rem; font-weight: 700;
  letter-spacing: 0.2em; text-transform: uppercase; color: rgba(13,13,13,0.4); margin-bottom: 1.5rem;
}
.engagement-headline {
  font-family: 'Fraunces', serif; font-weight: 900; font-style: italic;
  font-size: clamp(1.6rem, 3vw, 2.4rem); line-height: 1.1; letter-spacing: -0.02em;
  color: var(--ink); margin-bottom: 1rem;
}
.engagement-body { font-size: 0.92rem; line-height: 1.65; color: rgba(13,13,13,0.65); margin-bottom: 1.75rem; }
.engagement-link {
  font-family: 'JetBrains Mono', monospace; font-size: 0.72rem; font-weight: 700;
  letter-spacing: 0.05em; color: var(--ink); text-decoration: none;
  border-bottom: 2px solid var(--ink); padding-bottom: 2px; transition: opacity 0.12s;
}
.engagement-link:hover { opacity: 0.6; }
```

- [ ] **Step 5: Add Intersection Observer scroll reveals**

At the bottom of `<body>`, before `</body>`, add:

```html
<script>
  const revealEls = document.querySelectorAll('.work-section, .subscribe-section, .proof-section, .engagement-section');
  revealEls.forEach(el => {
    el.style.opacity = '0';
    el.style.transform = 'translateY(24px)';
    el.style.transition = 'opacity 0.5s ease, transform 0.5s ease';
  });
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.style.opacity = '1';
        entry.target.style.transform = 'translateY(0)';
        observer.unobserve(entry.target);
      }
    });
  }, { threshold: 0.08 });
  revealEls.forEach(el => observer.observe(el));
</script>
```

- [ ] **Step 6: Open index.html in browser and verify checklist**

Run: `open index.html`

Check:
1. Hero CTA reads "Read Dispatch →" — links to `dispatch.b.studio/subscribe`
2. Black Dispatch subscribe section appears between hero and work cards
3. beehiiv embed renders inside the subscribe section
4. "DLI, by Studio B →" link present below embed
5. Engagement section at bottom with plain text HubSpot link
6. No full-width iframe embed
7. Work cards + proof section fade in on scroll

- [ ] **Step 7: Commit**

```bash
git add index.html
git commit -m "feat(consulting): newsletter-first redesign — Dispatch embed + plain text meeting link"
```

---

## Task 2: capital.b.studio — DLI embed + Dispatch cross-subscribe

**Files:**
- Modify: `studio-b-ai/capital-b-studio/index.html`

The capital.b.studio site already exists and has the right visual identity (forest + paper palette). The changes are additive: add a DLI beehiiv subscribe embed, add a Dispatch cross-subscribe secondary section, and convert the HubSpot meeting iframe to a plain text link.

**Work in:** `/Users/kevin/dev/capital-b-studio/`

- [ ] **Step 1: Add DLI subscribe section after hero**

Read `docs/pub-ids.md` to get the DLI pub ID (`pub_e647b558-3a6e-4a29-87ff-c5fd2b566c35` — existing publication). Replace `DLI_PUB_ID` below.

Find the hero section closing `</section>` tag (after the `<a class="cta">Request LP materials</a>` CTA). Insert this section after it:

```html
<!-- DLI SUBSCRIBE -->
<section class="dli-subscribe" id="dli">
  <div class="dli-inner">
    <div class="dli-kicker">DLI, BY STUDIO B</div>
    <h2 class="dli-headline">The only open index for LMM direct lending.</h2>
    <p class="dli-body">Quarterly spread, structure, and recovery data across the global lower middle market. Market event commentary as it happens. We publish it because the benchmarks didn't exist — and LPs deserve to see the data before they commit capital.</p>
    <div class="beehiiv-embed-container">
      <iframe src="https://embeds.beehiiv.com/DLI_PUB_ID?slim=true" data-test-id="beehiiv-embed"
        height="52" frameborder="0" scrolling="no" style="margin: 0; border-radius: 0px !important;
        background-color: transparent; width: 100%;"></iframe>
    </div>
  </div>
</section>
```

Add CSS (insert in `<style>` block):

```css
/* DLI subscribe */
.dli-subscribe { background: var(--paper); color: var(--ink); padding: 4rem; border-bottom: 1px solid rgba(26,51,40,0.15); }
.dli-inner { max-width: 640px; }
.dli-kicker {
  font-family: 'JetBrains Mono', monospace; font-size: 0.5rem; font-weight: 700;
  letter-spacing: 0.2em; text-transform: uppercase; color: rgba(13,13,13,0.4); margin-bottom: 1.5rem;
}
.dli-headline {
  font-family: 'Fraunces', serif; font-weight: 900; font-style: italic;
  font-size: clamp(1.6rem, 3vw, 2.4rem); line-height: 1.1; letter-spacing: -0.02em;
  color: var(--ink); margin-bottom: 1rem;
}
.dli-body { font-size: 0.92rem; line-height: 1.65; color: rgba(13,13,13,0.65); margin-bottom: 2rem; }
.beehiiv-embed-container { margin-bottom: 1rem; }
```

- [ ] **Step 2: Replace HubSpot meeting iframe with plain text LP inquiry link**

Find and remove (in the `inquiry-section`):
```html
<div class="meetings-iframe-container" data-src="https://meetings.hubspot.com/bibelhausen/studio-b-capital-lp-inquiry?embed=true&color=1A3328"></div>
```

Replace with:
```html
<a href="https://meetings.hubspot.com/bibelhausen/studio-b-capital-lp-inquiry" class="inquiry-link" target="_blank" rel="noopener">
  Schedule an introductory call → meetings.hubspot.com/bibelhausen/studio-b-capital-lp-inquiry
</a>
```

Also remove the HubSpot meetings embed script tag if present:
```html
<script type="text/javascript" src="https://static.hsappstatic.net/MeetingsEmbed/ex/MeetingsEmbedCode.js"></script>
```

Add CSS:
```css
.inquiry-link {
  font-family: 'JetBrains Mono', monospace; font-size: 0.68rem; font-weight: 700;
  letter-spacing: 0.05em; color: var(--ink); text-decoration: none;
  border-bottom: 2px solid var(--ink); padding-bottom: 2px; display: inline-block;
  transition: opacity 0.12s;
}
.inquiry-link:hover { opacity: 0.6; }
```

- [ ] **Step 3: Add Dispatch cross-subscribe section before footer**

Find the `<footer>` tag. Before it, insert:

```html
<!-- DISPATCH CROSS-SUBSCRIBE -->
<section class="cross-subscribe" id="dispatch">
  <div class="cross-inner">
    <div class="cross-kicker">ALSO FROM KEVIN</div>
    <h3 class="cross-headline">Dispatch, by Studio B</h3>
    <p class="cross-body">Field notes from AI transformation engagements — for operators. Kevin also embeds at portfolio companies and publishes what he learns.</p>
    <a href="https://dispatch.b.studio/subscribe" class="cross-cta">Subscribe to Dispatch →</a>
  </div>
</section>
```

Add CSS:
```css
/* Cross-subscribe */
.cross-subscribe { background: var(--forest); color: var(--paper); padding: 4rem; border-top: 1px solid rgba(244,241,235,0.08); }
.cross-inner { max-width: 560px; }
.cross-kicker {
  font-family: 'JetBrains Mono', monospace; font-size: 0.5rem; font-weight: 700;
  letter-spacing: 0.2em; text-transform: uppercase; color: rgba(244,241,235,0.35); margin-bottom: 1rem;
}
.cross-headline {
  font-family: 'Fraunces', serif; font-weight: 900; font-style: italic;
  font-size: 1.6rem; line-height: 1.1; letter-spacing: -0.02em;
  color: var(--paper); margin-bottom: 0.75rem;
}
.cross-body { font-size: 0.88rem; line-height: 1.6; color: rgba(244,241,235,0.6); margin-bottom: 1.5rem; }
.cross-cta {
  font-family: 'JetBrains Mono', monospace; font-size: 0.6rem; font-weight: 700;
  letter-spacing: 0.1em; text-transform: uppercase; color: var(--yolk); text-decoration: none;
  border-bottom: 1px solid rgba(247,211,68,0.4); padding-bottom: 2px; transition: border-color 0.12s;
}
.cross-cta:hover { border-color: var(--yolk); }
```

- [ ] **Step 4: Open in browser and verify**

Run: `open index.html`

Check:
1. DLI subscribe section appears after the hero on paper background
2. beehiiv embed renders
3. LP inquiry section has plain text link, no iframe
4. Dispatch cross-subscribe section at bottom, forest background
5. No HubSpot meetings iframes remain

- [ ] **Step 5: Commit**

```bash
git add index.html
git commit -m "feat(capital): DLI subscribe embed + Dispatch cross-subscribe + plain text LP inquiry link"
```

---

## Task 3: build.b.studio — new repo and site

**Files:**
- Create: `studio-b-ai/build-b-studio/index.html`
- Create: `studio-b-ai/build-b-studio/CNAME`

The build.b.studio site is new. Create the GitHub repo first, then build the site. Register from the `studio-b-ai` org.

**NOTE:** Read `docs/pub-ids.md` for the Markdown pub ID before writing the beehiiv embed.

- [ ] **Step 1: Create the GitHub repo**

```bash
gh repo create studio-b-ai/build-b-studio --public --description "Build, by Studio B — entity site" --gitignore Node
cd /Users/kevin/dev
git clone git@github.com:studio-b-ai/build-b-studio.git
```

- [ ] **Step 2: Create CNAME file**

```bash
echo "build.b.studio" > /Users/kevin/dev/build-b-studio/CNAME
```

- [ ] **Step 3: Create index.html**

Create `/Users/kevin/dev/build-b-studio/index.html` with the full site. Replace `MARKDOWN_PUB_ID` with the pub ID from `docs/pub-ids.md`:

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Build, by Studio B — We ship on our own stack.</title>
  <meta name="description" content="Bolt, AcuOps, and Amplify run real operations. We build in public.">
  <meta property="og:type" content="website">
  <meta property="og:site_name" content="Build, by Studio B">
  <meta property="og:title" content="We ship on our own stack.">
  <meta property="og:description" content="Bolt, AcuOps, and Amplify run real operations. We build in public.">
  <meta property="og:url" content="https://build.b.studio">
  <meta name="twitter:card" content="summary_large_image">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,900;1,9..144,900&family=Inter:wght@400;600;800&family=JetBrains+Mono:wght@500;700&display=swap" rel="stylesheet">
  <style>
    *, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }
    :root {
      --yolk: #F7D344;
      --ink: #0D0D0D;
      --vermillion: #D94425;
      --paper: #F4F1EB;
      --forest: #1A3328;
    }
    html { scroll-behavior: smooth; }
    body { background: var(--ink); color: var(--paper); font-family: 'Inter', sans-serif; min-height: 100vh; }

    .stripe {
      height: 6px; width: 100%;
      background: repeating-linear-gradient(-45deg, var(--vermillion) 0, var(--vermillion) 8px, var(--ink) 8px, var(--ink) 16px);
    }

    /* Nav */
    .nav { padding: 1.5rem 4rem; display: flex; justify-content: space-between; align-items: center; border-bottom: 1px solid rgba(244,241,235,0.08); }
    .nav-mark { font-family: 'JetBrains Mono', monospace; font-size: 0.6rem; font-weight: 700; letter-spacing: 0.2em; text-transform: uppercase; color: rgba(244,241,235,0.45); text-decoration: none; }
    .nav-link { font-family: 'JetBrains Mono', monospace; font-size: 0.5rem; font-weight: 700; letter-spacing: 0.15em; text-transform: uppercase; color: rgba(244,241,235,0.35); text-decoration: none; transition: color 0.12s; }
    .nav-link:hover { color: var(--yolk); }

    /* Hero */
    .hero { padding: 5rem 4rem 4rem; max-width: 960px; border-bottom: 1px solid rgba(244,241,235,0.1); }
    .firm-label { font-family: 'JetBrains Mono', monospace; font-size: 0.52rem; font-weight: 700; letter-spacing: 0.2em; text-transform: uppercase; color: rgba(244,241,235,0.3); margin-bottom: 2rem; }
    h1 { font-family: 'Fraunces', serif; font-weight: 900; font-size: clamp(3rem, 6vw, 5.5rem); line-height: 0.93; letter-spacing: -0.03em; color: var(--yolk); margin-bottom: 1.75rem; }
    h1 em { font-style: italic; color: var(--vermillion); }
    .hero-body { font-size: 1.05rem; line-height: 1.65; color: rgba(244,241,235,0.65); max-width: 560px; margin-bottom: 2.5rem; }

    /* Subscribe */
    .subscribe-section { padding: 4rem; border-bottom: 1px solid rgba(244,241,235,0.08); }
    .subscribe-inner { max-width: 640px; }
    .subscribe-kicker { font-family: 'JetBrains Mono', monospace; font-size: 0.5rem; font-weight: 700; letter-spacing: 0.2em; text-transform: uppercase; color: rgba(247,211,68,0.45); margin-bottom: 1.5rem; }
    .subscribe-headline { font-family: 'JetBrains Mono', monospace; font-weight: 700; font-size: clamp(1.4rem, 2.5vw, 2rem); line-height: 1.2; color: var(--yolk); margin-bottom: 1rem; }
    .subscribe-body { font-size: 0.92rem; line-height: 1.65; color: rgba(244,241,235,0.6); margin-bottom: 2rem; }

    /* Products */
    .products-section { border-bottom: 1px solid rgba(244,241,235,0.08); }
    .products-head { padding: 3rem 4rem 2rem; border-bottom: 1px solid rgba(244,241,235,0.08); }
    .products-kicker { font-family: 'JetBrains Mono', monospace; font-size: 0.5rem; font-weight: 700; letter-spacing: 0.2em; text-transform: uppercase; color: rgba(244,241,235,0.3); margin-bottom: 1rem; }
    .products-head h2 { font-family: 'Fraunces', serif; font-weight: 900; font-style: italic; font-size: clamp(1.8rem, 3vw, 2.6rem); line-height: 1.05; letter-spacing: -0.02em; color: var(--paper); }
    .product-card { padding: 3rem 4rem; border-bottom: 1px solid rgba(244,241,235,0.08); display: grid; grid-template-columns: 200px 1fr; gap: 3rem; align-items: start; }
    .product-card:last-child { border-bottom: none; }
    .product-label { font-family: 'JetBrains Mono', monospace; font-size: 0.58rem; font-weight: 700; letter-spacing: 0.15em; text-transform: uppercase; color: var(--vermillion); margin-bottom: 0.5rem; }
    .product-name { font-family: 'Fraunces', serif; font-weight: 900; font-size: 1.6rem; line-height: 1; letter-spacing: -0.02em; color: var(--yolk); }
    .product-body { font-size: 0.92rem; line-height: 1.7; color: rgba(244,241,235,0.65); margin-bottom: 1.25rem; }
    .product-link { font-family: 'JetBrains Mono', monospace; font-size: 0.58rem; font-weight: 700; letter-spacing: 0.1em; text-transform: uppercase; color: var(--yolk); text-decoration: none; border-bottom: 1px solid rgba(247,211,68,0.3); padding-bottom: 1px; transition: border-color 0.12s; }
    .product-link:hover { border-color: var(--yolk); }

    /* Build notes */
    .notes-section { padding: 4rem; border-bottom: 1px solid rgba(244,241,235,0.08); }
    .notes-kicker { font-family: 'JetBrains Mono', monospace; font-size: 0.5rem; font-weight: 700; letter-spacing: 0.2em; text-transform: uppercase; color: rgba(244,241,235,0.3); margin-bottom: 2rem; }
    .note-item { padding: 1.5rem 0; border-bottom: 1px solid rgba(244,241,235,0.08); }
    .note-item:last-child { border-bottom: none; }
    .note-date { font-family: 'JetBrains Mono', monospace; font-size: 0.5rem; color: rgba(244,241,235,0.3); margin-bottom: 0.5rem; }
    .note-title { font-size: 0.96rem; font-weight: 600; color: var(--paper); margin-bottom: 0.4rem; }
    .note-excerpt { font-size: 0.82rem; line-height: 1.6; color: rgba(244,241,235,0.5); }
    .note-link { font-family: 'JetBrains Mono', monospace; font-size: 0.5rem; font-weight: 700; letter-spacing: 0.1em; text-transform: uppercase; color: var(--vermillion); text-decoration: none; margin-top: 0.5rem; display: inline-block; }

    /* Footer */
    .footer { padding: 2rem 4rem; border-top: 1px solid rgba(244,241,235,0.08); display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 1rem; }
    .footer-mark { font-family: 'JetBrains Mono', monospace; font-size: 0.5rem; font-weight: 700; letter-spacing: 0.15em; text-transform: uppercase; color: rgba(244,241,235,0.25); }
    .footer-links { display: flex; gap: 2rem; }
    .footer-link { font-family: 'JetBrains Mono', monospace; font-size: 0.5rem; font-weight: 700; letter-spacing: 0.12em; text-transform: uppercase; color: rgba(244,241,235,0.25); text-decoration: none; transition: color 0.12s; }
    .footer-link:hover { color: var(--yolk); }

    @media (max-width: 768px) {
      .hero, .subscribe-section, .products-head, .notes-section, .footer { padding: 3rem 1.5rem; }
      .product-card { grid-template-columns: 1fr; padding: 2rem 1.5rem; gap: 1rem; }
      .nav { padding: 1rem 1.5rem; }
    }
  </style>
</head>
<body>

<div class="stripe" aria-hidden="true"></div>

<nav class="nav">
  <a href="https://b.studio" class="nav-mark">b.</a>
  <a href="https://markdown.b.studio/subscribe" class="nav-link">Subscribe to Markdown →</a>
</nav>

<section class="hero">
  <p class="firm-label">Build, by Studio B</p>
  <h1>We ship on <em>our own</em> stack.</h1>
  <p class="hero-body">Bolt, AcuOps, and Amplify run real operations at Studio B's portfolio companies. We build in public — because you can't sell software you wouldn't run yourself.</p>
</section>

<!-- SUBSCRIBE -->
<section class="subscribe-section" id="subscribe">
  <div class="subscribe-inner">
    <div class="subscribe-kicker">Markdown, by Studio B</div>
    <h2 class="subscribe-headline">How we build AI-native software on real operations.</h2>
    <p class="subscribe-body">Behind-the-scenes of Bolt, AcuOps, and everything else Studio B ships. What we built, what broke, what we learned. Written for technical founders and operators who are also building.</p>
    <div class="beehiiv-embed-container">
      <iframe src="https://embeds.beehiiv.com/MARKDOWN_PUB_ID?slim=true" data-test-id="beehiiv-embed"
        height="52" frameborder="0" scrolling="no" style="margin: 0; border-radius: 0px !important;
        background-color: transparent; width: 100%;"></iframe>
    </div>
  </div>
</section>

<div class="stripe" aria-hidden="true"></div>

<!-- PRODUCTS -->
<section class="products-section" id="products">
  <div class="products-head">
    <div class="products-kicker">What we're building</div>
    <h2>Three products. All running in production.</h2>
  </div>

  <div class="product-card">
    <div>
      <div class="product-label">01</div>
      <div class="product-name">Bolt</div>
    </div>
    <div>
      <p class="product-body">Continuous goods WMS built for distributors who sell by the foot, yard, or kilogram. Container tracking, partial-cut inventory, DRP, ATP — the things that generic WMS counts wrong. Running at Heritage Fabrics in production.</p>
      <a href="https://bolt.b.studio" class="product-link">bolt.b.studio →</a>
    </div>
  </div>

  <div class="product-card">
    <div>
      <div class="product-label">02</div>
      <div class="product-name">AcuOps</div>
    </div>
    <div>
      <p class="product-body">Acumatica DevOps platform. Twelve health probes, post-publish test suite, CI/CD pipeline through the Customization API. VAR-facing. The infrastructure layer Acumatica doesn't ship.</p>
      <a href="https://acuops.com" class="product-link">acuops.com →</a>
    </div>
  </div>

  <div class="product-card">
    <div>
      <div class="product-label">03</div>
      <div class="product-name">Amplify</div>
    </div>
    <div>
      <p class="product-body">Outbound intelligence engine. Detects signals from X.com, LinkedIn, and GitHub activity — drafts newsletter issues and social content in a calibrated voice, routes through Slack for approval before anything publishes. We built this to run our own marketing. Now we sell it.</p>
      <a href="https://amplify.b.studio" class="product-link">amplify.b.studio →</a>
    </div>
  </div>
</section>

<!-- BUILD NOTES (placeholder — updated as Markdown issues publish) -->
<section class="notes-section" id="notes">
  <div class="notes-kicker">Recent build notes — from Markdown, by Studio B</div>
  <div class="note-item">
    <div class="note-date">Subscribe to get build notes as they publish</div>
    <div class="note-title">Behind the build: what ships and what breaks</div>
    <div class="note-excerpt">War stories from running Bolt, AcuOps, and Amplify on real operations. Subscribe to Markdown, by Studio B to get each issue.</div>
    <a href="https://markdown.b.studio/subscribe" class="note-link">Subscribe →</a>
  </div>
</section>

<footer class="footer">
  <div class="footer-mark">Build, by Studio B · b.studio</div>
  <nav class="footer-links">
    <a href="https://bolt.b.studio" class="footer-link">Bolt</a>
    <a href="https://acuops.com" class="footer-link">AcuOps</a>
    <a href="https://amplify.b.studio" class="footer-link">Amplify</a>
    <a href="https://b.studio" class="footer-link">Studio B</a>
  </nav>
</footer>

</body>
</html>
```

- [ ] **Step 4: Configure GitHub Pages**

In the GitHub web UI (or via `gh`):
```bash
gh api repos/studio-b-ai/build-b-studio --method PATCH -f "has_pages=true" 2>/dev/null || true
# Then go to: https://github.com/studio-b-ai/build-b-studio/settings/pages
# Set Source: Deploy from branch → main → / (root)
```

- [ ] **Step 5: Open in browser and verify**

```bash
open /Users/kevin/dev/build-b-studio/index.html
```

Check:
1. Ink background, yolk headline, vermillion accents
2. JetBrains Mono for subscribe section headline
3. Fraunces for hero h1
4. Three product cards: Bolt → bolt.b.studio, AcuOps → acuops.com, Amplify → amplify.b.studio
5. beehiiv embed in subscribe section

- [ ] **Step 6: Push and deploy**

```bash
cd /Users/kevin/dev/build-b-studio
git add .
git commit -m "feat: initial build.b.studio — Markdown subscribe + three products"
git push origin main
```

---

## Task 4: b.studio hub — fix stats, Amplify tag, replace §03 with entity lanes

**Files:**
- Modify: `studio-b-ai/b-studio-website/index.html`

Three targeted changes: (1) fix "4 products shipping" → "3 products in production" in the hero badges and the proof stats block, (2) fix "FOUR IN PRODUCTION" → "THREE IN PRODUCTION" and "LinkedIn Engine" → "Outbound Intelligence Engine" in the products section, (3) replace §03 "Work With Us" section with entity lanes.

**Work in:** `/Users/kevin/dev/b-studio-website/`

- [ ] **Step 1: Fix hero badge — 4 products → 3 products**

Find (line ~958):
```html
<span class="badge">4 products shipping</span>
```

Replace with:
```html
<span class="badge">3 products in production</span>
```

- [ ] **Step 2: Fix products section header**

Find (line ~970):
```html
<div class="ph-left">§ 02<br>— THE PRODUCTS —<br>FOUR IN PRODUCTION</div>
```

Replace with:
```html
<div class="ph-left">§ 02<br>— THE PRODUCTS —<br>THREE IN PRODUCTION</div>
```

- [ ] **Step 3: Fix Amplify tag**

Find (line ~1007):
```html
<div class="pb-tag">LinkedIn Engine</div>
```

Replace with:
```html
<div class="pb-tag">Outbound Intelligence Engine</div>
```

- [ ] **Step 4: Fix proof stats block — 4 products → 3 products**

Find in the proof section:
```html
<div class="ps-item"><div class="ps-n">4</div><div class="ps-l">Products in production</div></div>
```

Replace with:
```html
<div class="ps-item"><div class="ps-n">3</div><div class="ps-l">Products in production</div></div>
```

- [ ] **Step 5: Replace §03 "Work With Us" with entity lanes**

Find the entire services section. It begins with:
```html
<!-- SERVICES -->
<section class="services" id="services">
```
And ends with the closing `</section>` before `<div class="stripe forest"`.

Remove the entire services section and replace with:

```html
<!-- ENTITY LANES -->
<section class="entity-lanes" id="entities">
  <div class="el-head">
    <div class="el-kicker">§ 03<br>— ENTITIES —<br>THREE WAYS IN</div>
    <h2 class="el-h2">Three ways to <em>work with us.</em></h2>
  </div>
  <div class="el-grid">

    <div class="el-card consulting">
      <div class="el-num">01</div>
      <div class="el-name">Consulting, by Studio B</div>
      <p class="el-desc">We embed. We don't advise. AI transformation for operators and PE-backed companies — end to end, in production.</p>
      <div class="el-ctas">
        <a href="https://consulting.b.studio" class="el-primary">consulting.b.studio →</a>
        <a href="https://dispatch.b.studio/subscribe" class="el-newsletter">Read Dispatch, by Studio B →</a>
      </div>
    </div>

    <div class="el-card capital">
      <div class="el-num">02</div>
      <div class="el-name">Capital, by Studio B</div>
      <p class="el-desc">Non-bank lender for the global lower middle market. We built the lending index because the benchmarks didn't exist.</p>
      <div class="el-ctas">
        <a href="https://capital.b.studio" class="el-primary">capital.b.studio →</a>
        <a href="https://dli.b.studio/subscribe" class="el-newsletter">Read DLI, by Studio B →</a>
      </div>
    </div>

    <div class="el-card build">
      <div class="el-num">03</div>
      <div class="el-name">Build, by Studio B</div>
      <p class="el-desc">We ship on our own stack and publish the build. Bolt, AcuOps, and Amplify run real operations before they reach any client.</p>
      <div class="el-ctas">
        <a href="https://build.b.studio" class="el-primary">build.b.studio →</a>
        <a href="https://markdown.b.studio/subscribe" class="el-newsletter">Read Markdown, by Studio B →</a>
      </div>
    </div>

  </div>
</section>
```

Add CSS for entity lanes (insert in `<style>` block — remove or keep the `.services` CSS as appropriate):

```css
/* Entity lanes */
.entity-lanes { border-bottom: 3px solid var(--ink); }
.el-head { padding: 3rem 4rem 2rem; display: flex; align-items: flex-end; justify-content: space-between; border-bottom: 3px solid var(--ink); gap: 2rem; flex-wrap: wrap; }
.el-kicker { font-family: 'JetBrains Mono', monospace; font-size: 0.5rem; font-weight: 700; letter-spacing: 0.15em; text-transform: uppercase; color: rgba(13,13,13,0.35); white-space: pre-line; }
.el-h2 { font-family: 'Fraunces', serif; font-weight: 900; font-size: clamp(1.8rem, 3vw, 2.8rem); line-height: 1.05; letter-spacing: -0.02em; color: var(--ink); }
.el-h2 em { font-style: italic; color: var(--vermillion); }
.el-grid { display: grid; grid-template-columns: repeat(3, 1fr); }
.el-card { padding: 3rem; border-right: 3px solid var(--ink); }
.el-card:last-child { border-right: none; }
.el-num { font-family: 'JetBrains Mono', monospace; font-size: 0.5rem; font-weight: 700; letter-spacing: 0.2em; color: rgba(13,13,13,0.3); margin-bottom: 1rem; }
.el-name { font-family: 'Fraunces', serif; font-weight: 900; font-style: italic; font-size: 1.3rem; line-height: 1.05; letter-spacing: -0.02em; color: var(--ink); margin-bottom: 0.75rem; }
.el-desc { font-size: 0.85rem; line-height: 1.65; color: rgba(13,13,13,0.62); margin-bottom: 1.75rem; }
.el-ctas { display: flex; flex-direction: column; gap: 0.75rem; }
.el-primary { font-family: 'JetBrains Mono', monospace; font-size: 0.58rem; font-weight: 700; letter-spacing: 0.1em; text-transform: uppercase; color: var(--ink); text-decoration: none; transition: opacity 0.12s; }
.el-primary:hover { opacity: 0.6; }
.el-newsletter { font-family: 'JetBrains Mono', monospace; font-size: 0.52rem; font-weight: 700; letter-spacing: 0.08em; text-transform: uppercase; color: rgba(13,13,13,0.4); text-decoration: none; transition: color 0.12s; }
.el-newsletter:hover { color: var(--ink); }
@media (max-width: 768px) {
  .el-head { padding: 2rem 1.5rem; }
  .el-grid { grid-template-columns: 1fr; }
  .el-card { border-right: none; border-bottom: 3px solid var(--ink); padding: 2rem 1.5rem; }
  .el-card:last-child { border-bottom: none; }
}
```

- [ ] **Step 6: Update nav link from "Services" to "Entities" (if present)**

Search for a nav link pointing to `#services`:
```bash
grep -n "services\|Services" /Users/kevin/dev/b-studio-website/index.html | head -10
```
If found, update `href="#services"` → `href="#entities"` and link text "Services" → "Entities".

- [ ] **Step 7: Open in browser and verify**

```bash
open /Users/kevin/dev/b-studio-website/index.html
```

Check:
1. Hero badge says "3 products in production"
2. Products section header says "THREE IN PRODUCTION"
3. Amplify tag says "Outbound Intelligence Engine"
4. §03 section shows three entity lane cards, not the "Work With Us" pitch
5. Each card has entity site link + newsletter subscribe link
6. Proof stats show "3 products in production"
7. No Calendly anywhere

- [ ] **Step 8: Commit**

```bash
git add index.html
git commit -m "feat(hub): entity lanes replace Work With Us, fix stats, fix Amplify tag"
```

---

## Task 5: bibelhausen.com — new personal brand site

**Files:**
- Create: `kbibelhausen/bibelhausen-com/index.html`
- Create: `kbibelhausen/bibelhausen-com/CNAME`

This site lives under Kevin's personal GitHub account (`kbibelhausen`), not the `studio-b-ai` org. Visual identity: clean, minimal, type-led — **not** Billboard DNA. Palette: off-white `#F8F6F2` background, near-black `#161616` text, slate `#6B7280` secondary, no yolk, no stripes.

- [ ] **Step 1: Create the GitHub repo under kbibelhausen**

```bash
gh repo create kbibelhausen/bibelhausen-com --public --description "Kevin Bibelhausen — personal site"
cd /Users/kevin/dev
git clone git@github.com:kbibelhausen/bibelhausen-com.git
```

- [ ] **Step 2: Create CNAME**

```bash
echo "bibelhausen.com" > /Users/kevin/dev/bibelhausen-com/CNAME
```

- [ ] **Step 3: Create index.html**

Create `/Users/kevin/dev/bibelhausen-com/index.html`:

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Kevin Bibelhausen</title>
  <meta name="description" content="Kevin Bibelhausen — operator, builder, investor. Studio B, Capital, by Studio B.">
  <meta property="og:type" content="website">
  <meta property="og:site_name" content="Kevin Bibelhausen">
  <meta property="og:title" content="Kevin Bibelhausen">
  <meta property="og:description" content="Operator, builder, investor. Studio B, Capital, by Studio B.">
  <meta property="og:url" content="https://bibelhausen.com">
  <meta name="twitter:card" content="summary_large_image">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,900;1,9..144,900&family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
  <style>
    *, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }
    :root {
      --bg: #F8F6F2;
      --text: #161616;
      --secondary: #6B7280;
      --accent: #161616;
      --border: rgba(22,22,22,0.1);
    }
    html { scroll-behavior: smooth; }
    body { background: var(--bg); color: var(--text); font-family: 'Inter', sans-serif; min-height: 100vh; max-width: 680px; margin: 0 auto; padding: 0 2rem; }

    /* Nav */
    .nav { padding: 2.5rem 0 2rem; display: flex; justify-content: space-between; align-items: center; border-bottom: 1px solid var(--border); margin-bottom: 4rem; }
    .nav-name { font-family: 'Fraunces', serif; font-weight: 900; font-size: 1rem; letter-spacing: -0.02em; color: var(--text); }
    .nav-links { display: flex; gap: 1.5rem; }
    .nav-link { font-size: 0.78rem; color: var(--secondary); text-decoration: none; transition: color 0.12s; }
    .nav-link:hover { color: var(--text); }

    /* Hero */
    .hero { margin-bottom: 4rem; }
    .hero-name { font-family: 'Fraunces', serif; font-weight: 900; font-size: clamp(2.4rem, 5vw, 3.6rem); line-height: 0.95; letter-spacing: -0.04em; color: var(--text); margin-bottom: 1.25rem; }
    .hero-bio { font-size: 1.05rem; line-height: 1.7; color: var(--secondary); max-width: 520px; }

    /* Section */
    .section { margin-bottom: 3rem; padding-top: 3rem; border-top: 1px solid var(--border); }
    .section-label { font-size: 0.68rem; font-weight: 600; letter-spacing: 0.1em; text-transform: uppercase; color: var(--secondary); margin-bottom: 1.5rem; }
    .item { padding: 1.25rem 0; border-bottom: 1px solid var(--border); }
    .item:last-child { border-bottom: none; }
    .item-name { font-weight: 600; font-size: 0.96rem; color: var(--text); margin-bottom: 0.25rem; }
    .item-desc { font-size: 0.84rem; line-height: 1.6; color: var(--secondary); margin-bottom: 0.5rem; }
    .item-link { font-size: 0.78rem; color: var(--text); text-decoration: none; border-bottom: 1px solid var(--border); padding-bottom: 1px; transition: border-color 0.12s; }
    .item-link:hover { border-color: var(--text); }

    /* Subscribe links */
    .subscribe-list { display: flex; flex-direction: column; gap: 1rem; }
    .subscribe-item { display: flex; justify-content: space-between; align-items: center; padding: 1rem 0; border-bottom: 1px solid var(--border); }
    .subscribe-item:last-child { border-bottom: none; }
    .subscribe-name { font-weight: 500; font-size: 0.9rem; color: var(--text); }
    .subscribe-desc { font-size: 0.78rem; color: var(--secondary); margin-top: 0.15rem; }
    .subscribe-link { font-size: 0.75rem; color: var(--secondary); text-decoration: none; white-space: nowrap; margin-left: 1rem; transition: color 0.12s; }
    .subscribe-link:hover { color: var(--text); }

    /* Contact */
    .contact-list { display: flex; flex-direction: column; gap: 0.75rem; }
    .contact-item { font-size: 0.88rem; }
    .contact-label { color: var(--secondary); }
    .contact-link { color: var(--text); text-decoration: none; border-bottom: 1px solid var(--border); padding-bottom: 1px; }
    .contact-link:hover { border-color: var(--text); }

    /* Footer */
    .footer { padding: 2.5rem 0; border-top: 1px solid var(--border); margin-top: 2rem; }
    .footer-text { font-size: 0.72rem; color: var(--secondary); }

    @media (max-width: 480px) { body { padding: 0 1.25rem; } }
  </style>
</head>
<body>

<nav class="nav">
  <div class="nav-name">Kevin Bibelhausen</div>
  <div class="nav-links">
    <a href="#building" class="nav-link">Building</a>
    <a href="#follow" class="nav-link">Follow</a>
    <a href="#contact" class="nav-link">Contact</a>
  </div>
</nav>

<section class="hero">
  <h1 class="hero-name">Kevin<br>Bibelhausen</h1>
  <p class="hero-bio">Operator, builder, and investor. I run a portfolio of companies — distribution, SaaS, financial services — and Studio B's products run all of them. That's not a portfolio slide. It's the reason the software exists.</p>
</section>

<section class="section" id="building">
  <div class="section-label">What I'm building</div>
  <div class="item">
    <div class="item-name">Studio B</div>
    <div class="item-desc">AI-leveraged products and professional services. Bolt WMS, AcuOps, and Amplify — built first for the companies I operate.</div>
    <a href="https://b.studio" class="item-link">b.studio →</a>
  </div>
  <div class="item">
    <div class="item-name">Capital, by Studio B</div>
    <div class="item-desc">Private credit fund focused on LMM direct lending. We built the Studio B Direct Lending Index because the benchmarks didn't exist.</div>
    <a href="https://capital.b.studio" class="item-link">capital.b.studio →</a>
  </div>
</section>

<section class="section" id="follow">
  <div class="section-label">Follow the work</div>
  <div class="subscribe-list">
    <div class="subscribe-item">
      <div>
        <div class="subscribe-name">Dispatch, by Studio B</div>
        <div class="subscribe-desc">Field notes from consulting and AI transformation engagements</div>
      </div>
      <a href="https://dispatch.b.studio/subscribe" class="subscribe-link">Subscribe →</a>
    </div>
    <div class="subscribe-item">
      <div>
        <div class="subscribe-name">DLI, by Studio B</div>
        <div class="subscribe-desc">The open lower middle market lending index — quarterly data, market commentary</div>
      </div>
      <a href="https://dli.b.studio/subscribe" class="subscribe-link">Subscribe →</a>
    </div>
  </div>
</section>

<section class="section" id="contact">
  <div class="section-label">Get in touch</div>
  <div class="contact-list">
    <div class="contact-item">
      <span class="contact-label">Consulting inquiry — </span>
      <a href="https://consulting.b.studio" class="contact-link">consulting.b.studio</a>
    </div>
    <div class="contact-item">
      <span class="contact-label">LP inquiry — </span>
      <a href="https://capital.b.studio" class="contact-link">capital.b.studio</a>
    </div>
    <div class="contact-item">
      <span class="contact-label">Everything else — </span>
      <a href="mailto:kevin@b.studio" class="contact-link">kevin@b.studio</a>
    </div>
  </div>
</section>

<footer class="footer">
  <div class="footer-text">Kevin Bibelhausen · kevin@b.studio</div>
</footer>

</body>
</html>
```

- [ ] **Step 4: Configure GitHub Pages in repo settings**

```bash
# Enable GitHub Pages on the repo
# Go to: https://github.com/kbibelhausen/bibelhausen-com/settings/pages
# Set Source: Deploy from branch → main → / (root)
```

- [ ] **Step 5: Open in browser and verify**

```bash
open /Users/kevin/dev/bibelhausen-com/index.html
```

Check:
1. Off-white background, not yolk — no Billboard DNA
2. Clean, minimal, Inter + Fraunces only
3. "What I'm building" section: Studio B + Capital, by Studio B
4. "Follow the work" section: Dispatch + DLI subscribe links
5. Contact section: three channels correctly routed
6. No diagonal stripes, no product tiles, no beehiiv embed

- [ ] **Step 6: Push and deploy**

```bash
cd /Users/kevin/dev/bibelhausen-com
git add .
git commit -m "feat: initial bibelhausen.com — personal brand, clean minimal identity"
git push origin main
```

---

## Task 6: Update benchmarks.b.studio DLI button

**Files:**
- Modify: `studio-b-ai/benchmarks-b-studio/index.html`

The existing beehiiv embed on benchmarks.b.studio points to `brief.benchmarks.b.studio/subscribe`. The DLI newsletter is now at `dli.b.studio`. Update the embed and any subscribe links.

**Work in:** `/Users/kevin/dev/benchmarks-b-studio/`

- [ ] **Step 1: Find all references to the old domain**

```bash
grep -n "brief.benchmarks.b.studio\|benchmarks.*subscribe\|beehiiv" /Users/kevin/dev/benchmarks-b-studio/index.html
```

- [ ] **Step 2: Update the beehiiv embed src**

For each `<iframe src="...beehiiv...">` found, update the src to use the DLI pub ID from `docs/pub-ids.md`:
```html
<!-- OLD -->
<iframe src="https://embeds.beehiiv.com/OLD_PUB_ID?slim=true" ...>

<!-- NEW -->
<iframe src="https://embeds.beehiiv.com/pub_e647b558-3a6e-4a29-87ff-c5fd2b566c35?slim=true" ...>
```

- [ ] **Step 3: Update any text links to the subscribe page**

Replace any `href="https://brief.benchmarks.b.studio/subscribe"` with `href="https://dli.b.studio/subscribe"`.

- [ ] **Step 4: Verify**

```bash
open /Users/kevin/dev/benchmarks-b-studio/index.html
```

Confirm the subscribe link/embed points to DLI.

- [ ] **Step 5: Commit**

```bash
cd /Users/kevin/dev/benchmarks-b-studio
git add index.html
git commit -m "fix: update DLI subscribe link from brief.benchmarks.b.studio to dli.b.studio"
```

---

## Task 7: DNS — add GitHub Pages A records for new sites

**Files:**
- No code files — DNS configuration in GoDaddy.

Three domains need DNS configured for GitHub Pages. `build.b.studio` is a subdomain of `b.studio` (managed in GoDaddy). `bibelhausen.com` is a separate domain.

**NOTE:** DNS changes for newsletter CNAMEs (`dispatch.b.studio`, `dli.b.studio`, `markdown.b.studio`) are handled in the newsletter infrastructure plan. This task covers only the GitHub Pages records.

- [ ] **Step 1: Add CNAME for build.b.studio → GitHub Pages**

In GoDaddy DNS for `b.studio`:
```
Type: CNAME
Name: build
Value: studio-b-ai.github.io
TTL: 1 hour
```

- [ ] **Step 2: Configure GitHub Pages custom domain for build.b.studio**

In GitHub: repository settings → Pages → Custom domain → enter `build.b.studio` → Save.  
Wait for DNS propagation (up to 24h). GitHub will show a green checkmark when the domain is verified.

- [ ] **Step 3: Configure bibelhausen.com for GitHub Pages**

In GoDaddy DNS for `bibelhausen.com`, add four A records pointing to GitHub Pages IPs:
```
Type: A  Name: @  Value: 185.199.108.153
Type: A  Name: @  Value: 185.199.109.153
Type: A  Name: @  Value: 185.199.110.153
Type: A  Name: @  Value: 185.199.111.153
```
Also add:
```
Type: CNAME  Name: www  Value: kbibelhausen.github.io
```

Then in GitHub kbibelhausen/bibelhausen-com: Settings → Pages → Custom domain → `bibelhausen.com` → Save.

- [ ] **Step 4: Verify GitHub Pages HTTPS after propagation**

```bash
# After DNS propagates (up to 24h):
curl -I https://build.b.studio
curl -I https://bibelhausen.com
# Expect: HTTP/2 200
```

- [ ] **Step 5: Enable "Enforce HTTPS" in GitHub Pages settings**

For both repos, once the custom domain is verified: Settings → Pages → check "Enforce HTTPS".

---

## Self-Review

**Spec coverage check:**
- ✅ consulting.b.studio — newsletter-first, Dispatch embed, plain text meeting link (Task 1)
- ✅ capital.b.studio — DLI embed, Dispatch cross-subscribe, plain text LP inquiry link (Task 2)
- ✅ build.b.studio — new repo, ink background, three products, Markdown embed (Task 3)
- ✅ b.studio hub — stats fix, Amplify tag fix, entity lanes replace §03 (Task 4)
- ✅ bibelhausen.com — new repo, minimal identity, routes to entities (Task 5)
- ✅ benchmarks.b.studio DLI link update (Task 6)
- ✅ DNS for new GitHub Pages sites (Task 7)
- ✅ No Calendly anywhere — HubSpot meeting links only (Tasks 1, 2)
- ✅ Amplify on build.b.studio (Task 3)

**Open items from spec (not tasks here — tracked in spec §Open Questions):**
- bibelhausen.com visual identity noted as "distinct from Billboard" — the minimal off-white identity in Task 5 satisfies this; final palette is TBD until Kevin confirms
- capital.b.studio already exists as a repo — Task 2 modifies it, no new repo needed
- bibelhausen.com lives under `kbibelhausen` personal account, confirmed in Task 5
